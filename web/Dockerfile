FROM node:0.12

WORKDIR /usr/src/app

COPY . .

EXPOSE 3000

ENV NODE_ENV=production
# ENV API_ENDPOINT
# ENV UPLOAD_ENDPOINT
# ENV MAX_SIMULTANIOUS_UPLOAD_REQUESTS
# ENV CDN_PATH
# ENV GA_TRACKING_CODE
# ENV COOKIE_DOMAIN
# ENV FACEBOOK_APPID
# ENV GOOGLE_CLIENTID
# ENV PUSHER_KEY
# ENV FEEDBACK_ENDPOINT

CMD node app.js
